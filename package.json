{
  "name": "charity-dapp",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal single-page charity DApp with Hardhat + Solidity + Ethers v6",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "hardhat": "^2.22.10"
  }
}


